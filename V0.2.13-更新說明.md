# VLLM-CC-TOOLS-PROXY V0.2.13 更新說明

## 目的

V0.2.12 在 `LOG_PROTOCOL_SNIPPETS=true` 時，會把異常片段直接展開到容器主 Log。長內容可能被 Log Driver、終端或管理介面截斷，不利於完整分析。

V0.2.13 將這些內容改為依時間建立完整診斷暫存檔，主 Log 只保留檔案位置與摘要。

## 使用方式

```env
LOG_LEVEL=info
LOG_PROTOCOL_SNIPPETS=true
```

不新增其他 ENV。

## 診斷檔位置

```text
/tmp/vllm-cc-tools-proxy/protocol-snippets
```

檔名範例：

```text
20260806T074827.545Z__507e5d7e-121b-4c92-8283-8783bb594e3d__r02__original__a1b2c3d4.json
20260806T074900.120Z__507e5d7e-121b-4c92-8283-8783bb594e3d__r02__repair__e5f6a7b8.json
```

檔名包含：

- UTC 時間
- Request ID
- Managed Round
- original 或 repair
- 防碰撞識別碼

## 主 Log

主 Log 不再輸出：

```text
managed_final_response_anomaly_snippet
managed_final_response_input_protocol_snippet
```

只輸出：

```text
managed_final_response_diagnostic_file
```

內容包含：

```text
requestId
round
repair
reasons
output_snippet_count
input_snippet_count
file_path
file_bytes
file_sha256
created_at
```

若寫檔失敗，會記錄：

```text
managed_final_response_diagnostic_file_failed
```

寫檔失敗不會中止 Claude Code 任務，也不會改變原本的 Repair 流程。

## 診斷檔內容

JSON 檔會保存：

- Response ID、Model、Stop Reason、Block 類型
- 每個異常 Tag 的路徑、位移、行與列
- 完整且已遮蔽的異常輸出欄位
- System、Message History、Tool Description 中命中的完整遮蔽欄位
- original/repair 階段
- 內容長度、位元組數與 SHA-256

Bearer Token、API Key、Password、Secret、已知 Key Prefix 與 URL Userinfo 會先改成 `[REDACTED]` 才寫入檔案。

## Docker 取回方式

```bash
docker exec vllm-cc-tools-proxy \
  sh -lc 'ls -lt /tmp/vllm-cc-tools-proxy/protocol-snippets'

docker cp \
  vllm-cc-tools-proxy:/tmp/vllm-cc-tools-proxy/protocol-snippets \
  ./protocol-snippets
```

這是容器暫存資料，不保證在容器重建後仍存在。重現問題後應立即複製出來。

完成收集後恢復：

```env
LOG_PROTOCOL_SNIPPETS=false
```

## 相容性

本版只修改診斷輸出通道，未改變：

- Managed WebSearch／WebFetch 流程
- 非 Managed Tool 交還 Claude Code 的方式
- Final Response Guard 判定
- Repair 規則
- `MAX_TOOL_ROUNDS`
