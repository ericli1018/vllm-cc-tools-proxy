# VLLM-CC-TOOLS-PROXY V0.2.17 更新說明

## 目的

修正 Claude Code 啟用原生網路工具時，送出以下 Anthropic Server Tool Definition：

```json
{"type":"web_search_20250305","name":"web_search","max_uses":8}
{"type":"web_fetch_20250910","name":"web_fetch","max_uses":5}
```

由於這類工具沒有 `input_schema`，不支援 Anthropic Server Tool 的本地 vLLM 會在模型生成前回傳 `tools.N.input_schema Field required`。

## 修正內容

- 以 `web_search_*`、`web_fetch_*` Prefix 辨識目前與後續日期版本。
- 在 `/v1/messages/count_tokens` 與 `/v1/messages` 前轉成合法 Custom Tool Schema。
- `web_search` Schema 要求 `query`。
- `web_fetch` Schema 要求 `url`，並允許選用 `prompt`。
- Native Definition 不再原樣送往 vLLM。
- 一般 Custom Tool 與非 Web Tool 維持原樣。

## Policy

Proxy 在本地保留並執行：

- `max_uses`
- `allowed_domains`
- `blocked_domains`
- `max_content_tokens`

`max_uses` 超限會形成 `is_error=true` 的 Managed Tool Result，讓主模型改用其他有效動作，不會直接中止 Claude Code 任務。

`citations` 與 `user_location` 會被辨識，但本代理不宣稱能模擬 Anthropic 原生 Citation 或 Location Ranking。`allowed_callers`、動態過濾等新欄位不會送入 vLLM，並只以欄位數量記錄在安全診斷中。

## 診斷

```text
native_web_tools_normalized
```

只記錄：

- Native Tool 數量
- 套用 Policy 的工具種類
- 是否存在 max_uses
- 是否存在 Domain Policy
- Unsupported Field 數量

不記錄 Domain、URL、Prompt、搜尋結果、擷取內容或 Credential。

## 未修改

- Managed Loop 輪次與路由
- V0.2.14 Recovery Routing
- V0.2.15 Progress Semantics
- V0.2.16 Usage Preservation／Auto Compact Compatibility
- WebFetch Processor
- Protocol Diagnostic Files
- ENV 設定
