# V0.2.16 Anthropic Usage Preservation Implementation Plan

**Goal:** Supply a realistic `message_start.usage.input_tokens` to Claude Code for streamed managed turns.

**Architecture:** Add a small usage normalizer, preflight the Base token-count endpoint before opening managed SSE, inject the result into `ProgressStream`, observe upstream streaming usage, and keep count failures non-fatal.

## Tasks

- [x] Add failing usage normalization, progress-start, direct-stream, and proxy integration tests.
- [x] Implement Anthropic usage normalization and total-input calculation.
- [x] Add managed token preflight and initial SSE usage injection.
- [x] Add upstream usage observation and safe token-only diagnostics.
- [x] Add non-fatal token-count failure coverage.
- [x] Update release version, README, changelog, verification contract, and release notes.
- [x] Run full tests, syntax checks, manifest verification, clean extraction, and package integrity checks.
