# V0.2.28.2 Vision Empty Output Hotfix Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Restore reliable GLM Vision evidence after V0.2.28.1 and prevent empty Vision results from entering cache.

**Architecture:** Keep provider-native thinking controls only; do not inject `/nothink` into system prompts. Validate Vision output after reasoning stripping, retry one time on empty visible content with no tool calls, and fail closed after the retry so media cache never stores synthetic empty evidence. Preserve response-side reasoning stripping and add output-shape diagnostics. Update fallback progress wording and invalidate old visual/evidence caches.

**Tech Stack:** Node.js 22, native test runner, Ollama `/api/chat`, OpenAI-compatible chat completions, existing media cache/evidence contracts.

## Global Constraints
- Version: `0.2.28.2`.
- No new ENV variables.
- Keep `media-v7`; advance to `visual-v8` / `evidence-v4`.
- Do not alter PDF routing, Read.pages, Image wire diagnostics, Managed Loop, or Web tools.
- Formal package filename must not contain `(1)`.

### Task 1: Remove misplaced GLM `/nothink`
**Files:** `src/visual/vision-client.js`, `src/services/final-language-repair.js`, tests.
- [ ] Add failing tests asserting system prompts are unchanged for Ollama GLM with thinking disabled.
- [ ] Remove manual `/nothink` system-prefix helpers.
- [ ] Keep Ollama `think=false`, OpenAI-compatible `reasoning_effort=none`, and vLLM `chat_template_kwargs` behavior.
- [ ] Run focused tests.

### Task 2: Reject and retry empty Vision output
**Files:** `src/visual/vision-client.js`, `test/vision-client.test.js`.
- [ ] Add failing test: first empty output retries once and second visible output succeeds.
- [ ] Add failing test: two empty outputs raise `vision_empty_output` and never return synthetic evidence.
- [ ] Emit `vision_output_observed` for every response with safe counts only.
- [ ] Emit `vision_empty_output_retry` before the single retry.
- [ ] Run focused tests.

### Task 3: Progress and cache contracts
**Files:** `src/services/proxy-server.js`, `src/i18n/response-language.js`, `src/config.js`, tests.
- [ ] Add fallback-specific Traditional Chinese progress text when external repair fails and Base repair starts.
- [ ] Advance cache contracts to `media-v7 / visual-v8 / evidence-v4`.
- [ ] Update version assertions and deployment docs.

### Task 4: Release verification
**Files:** release docs, manifest, package metadata.
- [ ] Update `README.md`, `CHANGELOG.md`, `V0.2.28.2-更新說明.md`.
- [ ] Run full `npm test`, `npm run check`, `scripts/verify.sh`.
- [ ] Rebuild manifest and ZIP; verify ZIP integrity, SHA256, and zero `(1)` paths.
