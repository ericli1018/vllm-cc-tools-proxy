# VLLM-CC-TOOLS-PROXY V0.2.11 更新說明

## 核心變更

V0.2.11 將 Claude Code 的 `WebFetch(url, prompt)` 改為真正的「依 prompt 定向處理」流程：

```text
awesome-web-fetch 原始頁面
→ deterministic clean
→ protocol neutralization
→ 隔離的 WebFetch Content Processor
→ 可讀多行 VCC Tool Result
→ 主模型整理最終答案
```

原始網頁不再直接以 `JSON.stringify()` 的單行字串送入主模型。

## WebFetch Content Processor

Processor 使用獨立的 OpenAI-compatible `/v1/chat/completions` 請求：

- 不帶 Claude Code System Prompt。
- 不帶對話歷史。
- 不帶 tools 或 tool_choice。
- 使用 WebFetch 的 `prompt` 作為定向擷取需求。
- 只接受一般可見文字輸出。
- THINK 由布林設定控制。

## 精簡 ENV

```env
WEB_FETCH_PROCESSOR_ENABLED=true
WEB_FETCH_PROCESSOR_URL=
WEB_FETCH_PROCESSOR_MODEL=
WEB_FETCH_PROCESSOR_API_KEY=
WEB_FETCH_PROCESSOR_THINK=false
```

留白時：

- URL 由 `VLLM_BASE_URL` 自動推導 `/v1/chat/completions`。
- MODEL 使用目前 Claude Code Request 的 `model`。
- API KEY 只有在 URL 同樣留白、由 Base 自動推導時才繼承 `VLLM_BASE_API_KEY`；明確指定 Processor URL 時，必須另設 `WEB_FETCH_PROCESSOR_API_KEY`。
- THINK 預設 `false`。

使用同一個 Base vLLM 時，通常只需保留預設值。

## 主模型收到的 WebFetch Result

成功結果使用真正多行文字：

```text
[VCC_WEB_FETCH_RESULT_BEGIN version=2]

source:
requested_url: ...
final_url: ...
title: ...
status: 200
content_type: text/html
retrieved_at: ...
browser_rendered: true

processing:
mode: prompt_directed
truncated: false
warnings: none

result:

依 WebFetch prompt 整理後的內容

selected_evidence: none

[VCC_WEB_FETCH_RESULT_END]
```

主模型 System 只追加一次簡短說明，告知 `source`、`processing`、`result`、`selected_evidence` 的用途；不列出 XML Tool Tag。

## 清理與降級

原始頁面進 Processor 前會：

- 正規化換行。
- 移除控制字元。
- 中和已知模型協議標記與 Web/VCC 保留邊界標記。
- 壓縮重複行與明顯重複填充內容。
- 限制送入 Processor 的內容長度。

Processor 遇到 timeout、HTTP 錯誤、無效 JSON、空輸出、工具呼叫或協議標記時，不會把完整原始頁面塞回主模型，而是回傳 bounded cleaned excerpt：

```text
processing:
mode: fallback_excerpt
```

## 安全診斷

新增：

```text
web_fetch_processor_request
web_fetch_processor_response
web_fetch_processor_fallback
```

只記錄 endpoint host/path、HTTP status、耗時、THINK、是否驗證及字元數；不記錄 API Key、網頁內容、WebFetch prompt 或 Processor 產出。
