# VLLM-CC-TOOLS-PROXY V0.2.9 更新說明

## 版本定位

V0.2.9 修正三個在 V0.2.8 實際使用時暴露的邊界問題：

1. `awesome-web-fetch` Request／Response 契約不相容。
2. 單一 WebFetch 來源失敗會中止整個 Claude Code 任務。
3. Base vLLM 五分鐘等待限制與部分狀態只能等 Semantic Heartbeat 才顯示。

## WebFetch 契約

Proxy 現在直接呼叫 `WEB_FETCH_URL` 的完整設定值，不再自行附加 `/v1/fetch`。

```env
WEB_FETCH_URL=http://awesome-web-fetch:3000/
WEB_FETCH_API_KEY=
```

Request Body：

```json
{
  "urls": ["https://example.com/article"]
}
```

設定 `WEB_FETCH_API_KEY` 時，Proxy 只對 WebFetch Backend 加入：

```http
Authorization: Bearer <WEB_FETCH_API_KEY>
```

Response Adapter 支援：

```text
page_content
metadata.source
metadata.final_url
metadata.title
metadata.content_type
metadata.status_code
metadata.browser_rendered
```

並保留舊版物件回應格式的相容性。

## 可恢復的 WebFetch 錯誤

下列預期外部失敗不再直接終止 `/v1/messages`：

```text
HTTP 拒絕
robots 禁止
目標網站拒絕
Fetch Backend 驗證錯誤
```

Proxy 會產生對應的：

```json
{
  "type": "tool_result",
  "is_error": true
}
```

Base 模型可改選另一個搜尋結果或新聞來源繼續處理。非預期的 Proxy 程式錯誤仍會中止請求，不會被隱藏。

## Base vLLM Timeout

新增：

```env
VLLM_BASE_CONNECT_TIMEOUT_MS=10000
VLLM_BASE_HEADERS_TIMEOUT_MS=900000
VLLM_BASE_BODY_TIMEOUT_MS=900000
```

定義：

| 設定 | 用途 | 預設值 |
|---|---|---:|
| `VLLM_BASE_CONNECT_TIMEOUT_MS` | TCP／TLS 連線建立 | 10 秒 |
| `VLLM_BASE_HEADERS_TIMEOUT_MS` | 等待 Base vLLM HTTP Headers | 15 分鐘 |
| `VLLM_BASE_BODY_TIMEOUT_MS` | Response Body 相鄰資料段的最大閒置時間 | 15 分鐘 |

錯誤會區分為：

```text
vllm_connect_timeout
vllm_headers_timeout
vllm_body_timeout
vllm_connection_refused
vllm_connection_reset
vllm_network_unreachable
```

不再把所有失敗都顯示成泛用的 `vLLM upstream is unavailable`。

## 即時進度更新

V0.2.9 的規則：

```text
狀態發生變化
→ 立即送出 Anthropic content_block_delta

狀態沒有變化
→ 每 30 秒送 Semantic Heartbeat
```

在 `PROGRESS_VISIBLE_AFTER_MS` 門檻內發生的狀態不再被丟棄。Proxy 會保存最新 Pending Snapshot；任務超過門檻仍在執行時，立即顯示最新狀態。

Base vLLM Lifecycle 現在會即時顯示：

```text
正在將內容送往主模型…
主模型已接受請求，正在準備輸出…
主模型已開始回傳結果…
```

30 秒 Heartbeat 不再負責傳遞狀態改變，只負責維持同一狀態下的 Claude Code Semantic Stream 活性。

## 診斷事件

新增／強化：

```text
progress_state_changed
progress_sse_sent
base_upstream_request_failed
web_fetch_upstream_request
web_fetch_upstream_response
web_fetch_upstream_rejected
```

Log 不包含 API Key、完整 Prompt、抓取網頁內容、PDF 文字或 Base64。

## 升級設定

Compose 使用者需加入或保留：

```env
VLLM_BASE_CONNECT_TIMEOUT_MS=10000
VLLM_BASE_HEADERS_TIMEOUT_MS=900000
VLLM_BASE_BODY_TIMEOUT_MS=900000
WEB_FETCH_API_KEY=
```

既有 `PROGRESS_HEARTBEAT_MS=30000` 可維持不變。
