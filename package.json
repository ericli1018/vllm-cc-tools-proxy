{
  "name": "vllm-cc-tools-proxy",
  "version": "0.29.9",
  "private": true,
  "type": "module",
  "description": "Transparent Claude Code gateway with bounded media, visual and managed-tool compatibility workflows",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-reporter=spec",
    "check": "node scripts/check-syntax.js"
  }
}
