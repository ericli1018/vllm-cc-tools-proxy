# VLLM-CC-TOOLS-PROXY V0.2.8 更新說明

日期：2026-08-06

## 主要修正

### 1. 顯示目前處理的檔案

Managed Progress 現在會顯示安全的檔案 basename：

```text
目前處理進度：
檔案：GW305_N101_20260519-board.pdf｜圖片 4/15｜狀態：正在使用視覺模型分析圖片…
```

檔名來源依序為：

```text
source.filename
block title/name
對應 Read tool_use 的 input.file_path
PDF #N / 圖片 #N
```

完整本機路徑不會顯示在 Claude Code Progress，也不會寫入進度傳送紀錄。

### 2. 頁面、圖片、批次與區段進度

PDF Progress 可顯示：

```text
頁面 8/15（53%）
批次 2/4
區段 1/2
```

Claude Code 將同一 Read 結果拆成多個圖片 Block 時，可顯示：

```text
圖片 7/15
```

當檔案資訊不可取得時，退回 `PDF #N` 或 `圖片 #N`，不猜測路徑。

### 3. Semantic Heartbeat

V0.2.7 只有：

```text
event: ping
```

這可維持 TCP／反向代理連線，但部分 Claude Code 版本不把它視為有效 Model Chunk，仍可能在五分鐘後顯示：

```text
API Error: Stream idle timeout - no chunks received
```

V0.2.8 增加真正的 Anthropic Text Delta：

```env
PROGRESS_HEARTBEAT_MS=30000
```

範例：

```text
檔案：GW305_N101_20260519-board.pdf｜狀態：主模型仍在處理中，已等待 30 秒…
```

Heartbeat 覆蓋：

- PDF／圖片處理。
- Visual Model 等待。
- Base vLLM Response Header 等待。
- Base vLLM 第一個 Thinking／Text／Tool Event 等待。

收到第一個真正的 Base Model Content Block 後，Progress Block 會先正常關閉，再串流模型內容。

### 4. SSE 寫入確認與 Backpressure

新增：

```env
SSE_DRAIN_TIMEOUT_MS=10000
```

若 Socket Backpressure 長時間沒有 `drain`，不再永久卡住整個 SSE Queue。

Log 分成：

```text
managed_task_progress (`delivery_status=requested`)
progress_sse_sent
progress_sse_backpressure
```

因此 Server Log 出現進度文字，不再被誤認為 Claude Code 一定已經收到；只有 `progress_sse_sent` 才表示 Text Delta 已完成寫入。

### 5. Base vLLM TTFT 觀測

新增：

```text
base_upstream_request_start
base_upstream_headers_received
base_upstream_first_event
base_upstream_stream_completed
```

欄位包含：

- Request bytes。
- Message count。
- Evidence bytes。
- Header wait milliseconds。
- First event wait milliseconds。
- Total stream milliseconds。

不記錄 Prompt、文件內容、Base64 或 API Key。

## 新增 ENV

```env
PROGRESS_VISIBLE_AFTER_MS=1500
PROGRESS_PING_INTERVAL_MS=5000
PROGRESS_HEARTBEAT_MS=30000
SSE_DRAIN_TIMEOUT_MS=10000
```

建議先使用預設值。若 Base vLLM 首 Token 經常超過五分鐘，不應只提高 Claude Code Timeout；應保留 Semantic Heartbeat 並檢查 TTFT Log。

## 相容性

- 一般透明 Bypass 不進入 ProgressStream。
- 快速 Managed Request 不會只為顯示完成訊息而建立 Progress Block。
- Cache Hit 仍不重新執行逐頁 Visual 分析。
- V0.2.2～V0.2.7 的舊 Progress History 仍會清理。
- V0.2.7 Structured Evidence Contract 保持不變。
