# VLLM-CC-TOOLS-PROXY V0.2.12 更新說明

## 目的

V0.2.12 是 Protocol Anomaly Logging 強化版。當主模型最終回應觸發 Final Response Guard 時，除了既有的原因與 Tag 數量，現在可以選擇記錄異常發生位置附近的實際片段，方便判斷模型為什麼持續產生 `function_results`、`tool_response`、錯誤 Thinking 邊界或其他協議內容。

## 啟用方式

```env
LOG_LEVEL=info
LOG_PROTOCOL_SNIPPETS=true
```

重新啟動 Proxy 後生效。預設為：

```env
LOG_PROTOCOL_SNIPPETS=false
```

建議只在重現與分析期間啟用，完成後恢復為 `false`。

## 新增 Log 事件

### `managed_final_response_anomaly_snippet`

每個異常位置各產生一筆，包含：

- `round`：Managed Loop 輪次。
- `repair`：`false` 表示原始回應；`true` 表示受控修正後的回應。
- `reason`：`control_tag_leak`、`final_answer_in_thinking` 或 `missing_visible_text`。
- `response_id`、`response_model`、`stop_reason`。
- `block_index`、`block_type`、`field`。
- `tag_name`、`tag_raw`。
- `char_index`、`line`、`column`。
- `snippet`、`context_before`、`context_after`。
- `content_chars`、`content_bytes`、`content_sha256`。

若答案全部停留在 Thinking，會記錄有限長度的：

```text
excerpt_head
excerpt_tail
omitted_chars
```

若回應沒有可見 Text，會記錄有限長度的：

```text
block_preview
preview_truncated
```

### `managed_final_response_input_protocol_snippet`

原始回應需要 Repair 時，Proxy 會同步掃描當輪送進 Base vLLM 的：

- System Prompt。
- Message History。
- Tool Definitions。

每個命中位置都會輸出：

```text
scope
path
message_index
role
tag_name
tag_raw
snippet
line
column
```

這可直接判斷異常 Tag 是：

1. 只由模型生成。
2. 已存在於 Claude Code System Prompt。
3. 來自歷史 Thinking／Tool Result。
4. 來自 Tool Description 的範例或格式說明。

### `managed_final_response_diagnostic_summary`

記錄該次異常輸出的片段數量與輸入端協議片段數量：

```text
output_snippet_count
input_snippet_count
```

## 安全界線

V0.2.12 不會把完整 System Prompt、完整對話、完整 WebFetch 頁面或完整模型回應寫入 Log。

詳細片段會限制長度，並在輸出前遮蔽：

- Bearer Token。
- API Key。
- Access/Auth Token。
- Password／Secret。
- `sk-`、`sk_`、`hf_` 類 Key。
- URL Userinfo。

遮蔽後統一顯示：

```text
[REDACTED]
```

## 建議提供的 Log

分析時請提供同一個 `requestId` 的以下事件：

```text
incoming_protocol_inventory
managed_final_response_inspected
managed_final_response_anomaly_snippet
managed_final_response_input_protocol_snippet
managed_final_response_diagnostic_summary
managed_final_response_repair_start
managed_final_response_repair_success
managed_final_response_rejected
```

這一組 Log 可以還原：

```text
輸入端有哪些協議方言
→ 模型在哪個 Block、哪個位置產生異常
→ 第一次 Repair 是否消除異常
→ 修正後是否改成另一種錯誤
```

## 行為不變

- 正式 `tool_use` 仍優先執行，不會被 Final Guard 擋掉。
- 只有沒有 Tool Call、準備當成最終答案的回應才會接受 Final Guard 檢查。
- 仍只允許一次 tools-disabled 受控修正。
- `LOG_PROTOCOL_SNIPPETS=false` 時，維持 V0.2.11 的安全 count-only Log 行為。

## 驗證結果

```text
Node tests:        177 passed / 0 failed
JavaScript syntax: 68 files passed
Deployment checks: passed
Docker Compose:    runtime unavailable; static contract passed
```
