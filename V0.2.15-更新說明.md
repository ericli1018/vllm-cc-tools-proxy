# VLLM-CC-TOOLS-PROXY V0.2.15 更新說明

## 目標

V0.2.15 修正進度文案的狀態語意。Proxy 只描述目前這一輪模型請求及轉交狀態，不再以「處理完成」暗示 Claude Code 的完整任務已結束。

本版不修改：

- Managed WebSearch／WebFetch 執行流程。
- V0.2.14 Recovery Routing。
- 非 Managed Tool 交還 Claude Code 的規則。
- WebFetch Processor。
- `MAX_TOOL_ROUNDS`。
- Protocol Diagnostic File。

## 回應狀態分類

### 產生 Claude Code 下一步工具

單一工具：

```text
主模型已產生下一步 Write；正在交還 Claude Code 執行…
```

多個工具：

```text
主模型已產生下一步工具；正在交還 Claude Code 執行…
```

Phase：

```text
handoff_to_claude_code
```

這表示目前 Proxy Turn 已可交還，但 Claude Code 整體任務仍會繼續。

### 產生本輪可見回答

```text
主模型已完成本輪回答；正在回傳結果…
```

Phase：

```text
returning_visible_response
```

### 其他有效模型輸出

```text
主模型已完成本輪輸出；正在回傳結果…
```

Phase：

```text
returning_model_output
```

## 終止範圍 Metadata

進度狀態加入：

```text
terminal_for_proxy=true
terminal_for_claude_task=false
```

`terminal_for_proxy=true` 只代表目前 Proxy API Turn 可以回傳。

`terminal_for_claude_task=false` 表示 Proxy 不宣稱 Claude Code 外層任務已完成。

## Direct Streaming

收到第一個 Base vLLM Content Block 時，依類型顯示：

```text
text      -> 主模型已開始回傳本輪回答…
thinking  -> 主模型已開始回傳思考內容…
tool_use  -> 主模型已開始回傳下一步工具…
other     -> 主模型已開始回傳本輪輸出…
```

對應 Phase：

```text
streaming_visible_response
streaming_thinking
streaming_tool_action
streaming_model_output
```

## Heartbeat

一般等待狀態改為：

```text
主模型仍在處理本輪請求，已等待 30 秒…
```

檔案分析完成後等待 Base vLLM 時，保留安全檔名並使用相同的「本輪請求」語意。

## ENV

V0.2.15 不新增 ENV。
