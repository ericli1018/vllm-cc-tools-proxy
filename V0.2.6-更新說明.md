# VLLM-CC-TOOLS-PROXY V0.2.6 更新說明

## 主要修正

### 進度標題

新版本顯示：

```text
目前處理進度：
```

不再新產生 `VLLM-CC-TOOLS-PROXY 進度：`。Proxy 仍會清除舊版本的可見進度區塊與 V0.2.2 Sentinel 歷史。

### PDF 頁面與批次

Proxy 處理 Claude Code 實際送入的完整 PDF Payload：

```text
收到 4 頁  -> 處理 4 頁 -> 1 個 Visual Batch
收到 20 頁 -> 處理 20 頁 -> 5 個 Visual Batch
```

預設每批四頁。Progress 與 Log 會包含收到頁數、完成頁數及 Batch 數量。Claude Code UI 的 `pages 1-20` 參數在 Proxy 之前由 `Read` Tool 執行；若 Wire Payload 沒有原始頁碼 metadata，Proxy 不自行推測。

### Crop 錯誤內部恢復

Visual Model 產生過小或不合法的 Crop 時，不再向 Claude Code 回傳頂層 `422 API Error`。Proxy 將安全的 Tool Error 回給 Visual Model，讓模型修正 bbox 或直接使用既有證據完成分析。

```text
Visual Crop 驗證失敗
-> internal tool result
-> 模型修正或完成
-> 最多兩輪
-> 最後關閉 Crop Tool 完成分析
```

真正的 Proxy 程式缺陷仍會回報，不會被誤吞。

### Qwen3.6 Thinking 控制

新增：

```env
VLLM_VISION_PROVIDER=vllm
VLLM_VISION_THINK=false
```

`VLLM_VISION_PROVIDER` 只接受 `vllm` 或 `ollama`；`VLLM_VISION_THINK` 只接受 `true` 或 `false`。

#### Ollama Native

```env
VLLM_VISION_PROVIDER=ollama
VLLM_VISION_URL=http://192.168.10.169:11434
VLLM_VISION_MODEL=qwen3.6:27b-q4_K_M-cc
VLLM_VISION_API_KEY=ollama
VLLM_VISION_THINK=false
```

Proxy 呼叫 `/api/chat` 並帶入：

```json
{"think":false}
```

#### vLLM OpenAI-compatible

```env
VLLM_VISION_PROVIDER=vllm
VLLM_VISION_THINK=false
```

Proxy 呼叫 `/v1/chat/completions` 並帶入：

```json
{
  "reasoning_effort":"none",
  "chat_template_kwargs":{"enable_thinking":false}
}
```

Thinking 狀態與 Provider 已加入 Cache Key；切換設定會自然重新分析，不會沿用不同模式的舊 Cache。

## 升級提醒

使用 Ollama 的現有部署必須新增：

```env
VLLM_VISION_PROVIDER=ollama
VLLM_VISION_THINK=false
```

未設定 Provider 時，為保持舊版相容性，預設仍為 `vllm`。
