# VLLM-CC-TOOLS-PROXY V0.2.16 更新說明

## 目標

V0.2.16 修正 Managed Streaming 回給 Claude Code 的 Anthropic Usage 契約。

V0.2.15 為了立即建立進度 SSE，會先產生 synthetic `message_start`，但其中固定為：

```json
{"usage":{"input_tokens":0,"output_tokens":0}}
```

這會讓 Claude Code 低估目前 Context，導致 `CLAUDE_CODE_AUTO_COMPACT_WINDOW` 可能無法在正確時間觸發。

## Token Preflight

Streamed Managed Request 在開啟進度 SSE 前，先使用既有 Base vLLM Token Count Endpoint：

```text
POST /v1/messages/count_tokens
```

取得：

```text
input_tokens
```

再建立 downstream：

```text
message_start.message.usage.input_tokens
```

因此 Claude Code 不再收到固定的 `input_tokens=0`。

## Usage 欄位

Proxy 會正規化並保留：

```text
input_tokens
cache_creation_input_tokens
cache_read_input_tokens
output_tokens
server_tool_use
```

本地 vLLM 沒有 Anthropic Prompt Cache Usage 時，cache 欄位維持 `0`。

最終 `message_delta` 繼續使用 Base Response 的真實 `output_tokens`。

## Direct Streaming

Direct transformed streaming 仍只會產生一個 downstream `message_start`。

Upstream 的：

```text
message_start.usage
message_delta.usage
```

會被觀察並記錄 Token-only 診斷，但不會再轉送第二個 `message_start`。

## 診斷事件

新增：

```text
managed_usage_preflight_succeeded
managed_usage_preflight_failed
managed_response_usage_observed
managed_stream_usage_observed
```

只包含 Token 數量、差值與錯誤代碼，不包含 Prompt、Tool Result 或敏感內容。

## 失敗降級

若 `/v1/messages/count_tokens` 不存在、回傳錯誤或缺少 `input_tokens`：

```text
記錄 managed_usage_preflight_failed
→ 使用 input_tokens=0 fallback
→ 繼續原始 Claude Code Turn
```

Token Preflight 失敗不會中止 Write、Read、Bash、Edit、Task、WebSearch 或 WebFetch。

## Claude Code 建議設定

224,000 Context、16,384 最大輸出時：

```bash
export CLAUDE_CODE_AUTO_COMPACT_WINDOW=180000
export CLAUDE_CODE_MAX_OUTPUT_TOKENS=16384
```

`200000` 只留下約 7,616 tokens 的單輪安全空間，容易被大型 Tool Result 跨越。

## 未變更項目

V0.2.16 沒有修改：

- V0.2.15 進度文案與狀態語意。
- V0.2.14 Recovery Routing。
- Thinking-only 分流。
- Tool 保留規則。
- Managed WebSearch／WebFetch 執行。
- WebFetch Processor。
- Protocol Diagnostic Files。
- `MAX_TOOL_ROUNDS`。

## ENV

V0.2.16 不新增 Proxy ENV。
