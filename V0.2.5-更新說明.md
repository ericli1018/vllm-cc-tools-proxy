# VLLM-CC-TOOLS-PROXY V0.2.5 更新說明

## 核心修正

V0.2.5 解決 Claude Code 在後續 Tool Round 重新送出完整 Message History 時，同一份 PDF 或圖片被 Proxy 重複分析的問題。

```text
第一次 Read(PDF)
→ Cache Miss
→ Poppler / Visual vLLM
→ 寫入持久化 Cache

Write / Bash / 其他 Tool 完成後的下一輪
→ 歷史中再次出現同一 PDF Base64
→ Cache Hit
→ 不進 Managed Queue
→ 不呼叫 Visual vLLM
→ 不顯示第二次進度
→ 直接把 Cached Markdown 送給 Base vLLM
```

## 持久化 Cache

新增 Compose Named Volume：

```text
proxy-data:/var/lib/vllm-cc-tools-proxy
```

保存內容：

- 已正規化的 PDF Markdown。
- 圖片與頁面 Visual Analysis。
- Page、Parser、Warning、Truncated 等 Metadata。
- Cache 建立與最後使用時間。

不保存：

- 原始 Base64。
- Claude Code 完整 Request。
- API Key。
- 使用者完整 Prompt。

## Cache 容量

```env
MEDIA_CACHE_MAX_MB=0
```

規則：

- `0`：不設定 Proxy 容量上限，使用到檔案系統回報不足。
- `>0`：以 MiB 為單位限制 Cache，超過時依 LRU 清理。
- 負數或非數字：Proxy 拒絕啟動。
- Compose 預設為 `0`。

保存期限仍由 `RESOURCE_PROFILE` 決定：

| Profile | 最後使用後保存目標 | 未覆寫時容量 |
|---|---:|---:|
| `small` | 3 天 | 512 MiB |
| `default` | 7 天 | 2048 MiB |
| `large` | 30 天 | 10240 MiB |

Compose 預設明確設定 `MEDIA_CACHE_MAX_MB=0`，因此一般 Compose 部署使用檔案系統容量，而不是 Profile 容量。

## 磁碟空間不足

若 Cache 寫入回報 `ENOSPC` 或 `EDQUOT`：

```text
完成的文件分析仍用於當前 Request
→ Cache 寫入失敗
→ 不留下不完整 Entry
→ /health 變為 degraded
→ 後續相同文件可能需要重新分析
```

## 去重

- 同一 Request 內相同媒體只建立一份暫存檔。
- 同一 Request 內相同媒體只分析一次。
- 同一 Proxy Process 中，並行 Request 的相同媒體共用一個 Singleflight Analysis Job。
- 單一等待者取消不會中止其他等待者仍需要的分析。
- 所有等待者都取消時才會 Abort 共用分析。

## Managed Keepalive

不可見 Anthropic SSE `ping` 現在持續涵蓋：

```text
Managed Queue
→ PDF / Image Parser
→ Visual vLLM
→ Base vLLM TTFT
→ Base vLLM Token Stream 停頓
→ message_stop
```

Ping 只在完整 SSE Frame 邊界插入，不會切斷 JSON，也不會顯示為 Claude Code 文字。

## Claude Code Probe

新增本地相容路由：

```text
HEAD /api/hello → 200，無 Body
GET  /api/hello → 200，{"message":"hello"}
```

不再把這兩個 Probe Bypass 到 Base vLLM。

## Health

`GET /health` 增加：

```json
{
  "cache": {
    "entries": 42,
    "bytes": 183500800,
    "max_bytes": 0,
    "limit_mode": "filesystem",
    "write_available": true,
    "inflight_analyses": 1
  }
}
```
