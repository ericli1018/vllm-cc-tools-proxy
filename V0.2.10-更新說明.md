# VLLM-CC-TOOLS-PROXY V0.2.10 更新說明

## 版本定位

```text
V0.2.10
Protocol Provenance Guard
Managed Final Response Repair
Lazy Web Progress
```

本版處理兩個問題：

1. WebSearch／WebFetch 完成後，Base 模型偶爾輸出 `</function_results>`、`</tool_response>` 等非目前 Laguna Protocol 的標記，導致正式答案仍落在 Thinking Channel。
2. Web-only Request 只是提供工具、但模型直接回答時，Claude Code 仍過早顯示「目前處理進度」。

## 一、協議來源診斷

新增安全診斷事件：

```text
incoming_protocol_inventory
managed_tool_result_protocol_inventory
managed_final_response_inspected
managed_final_response_repair_start
managed_final_response_repair_success
managed_final_response_rejected
```

只記錄：

```text
Tag 名稱與數量
Content Block 類型
Thinking／Text Bytes
Stop Reason
Repair 狀態
```

不記錄：

```text
System Prompt 原文
搜尋結果原文
抓取網頁內容
最終答案原文
API Key／Authorization
```

## 二、擴充控制標記識別

現在可識別並中和：

```text
function_result / function_results
function_call / function_calls
tool_result / tool_results
tool_response / tool_responses
tool_call / tool_calls
think / thinking / analysis
arg_key / arg_value / function / parameter
ChatML im_start / im_end
antml:function_results 等 namespaced 變形
```

## 三、Managed Tool Result 中和

WebSearch／WebFetch 回傳的所有字串，會在建立 Anthropic `tool_result` 前遞迴檢查並中和。

例如外部網頁包含：

```text
</tool_response>
</function_results>
<tool_call>
```

送往 Base vLLM 時會變成：

```text
&lt;/tool_response&gt;
&lt;/function_results&gt;
&lt;tool_call&gt;
```

因此外部內容只能作為資料，不會成為 Chat Template 控制語法。

## 四、Managed Final Response Guard

Managed Tool Loop 最後一輪不再以「沒有 tool_use」直接視為完成。

Proxy 會檢查：

```text
Thinking／Text 是否含控制標記
是否存在可見 Text Block
正式答案是否整段留在 Thinking
Stop Reason 與 Block 結構
```

若第一次最終回應異常：

```text
移除 tools 與 tool_choice
保留已取得的搜尋／網頁證據
加入一次高優先格式修正要求
重新呼叫 Base 模型
```

修正成功後才回傳 Claude Code。

若第二次仍異常：

```text
final_response_protocol_mismatch
```

Raw Tag 不會直接顯示給使用者。

## 五、Web-only Lazy Progress

以前只要 Request 提供 WebSearch／WebFetch，第一輪就可能顯示：

```text
● 目前處理進度：
  正在請主模型規劃下一步…
```

V0.2.10 改為：

```text
第一輪直接回答且未超過 Heartbeat 門檻
→ 不顯示「目前處理進度」

第一輪真的呼叫 WebSearch／WebFetch
→ 立即顯示工具狀態

第一輪尚未呼叫工具但等待超過 PROGRESS_HEARTBEAT_MS
→ 顯示 Semantic Heartbeat

Media、Queue、Retry、Final Repair
→ 狀態改變時立即顯示
```

第一個真正的 Managed Tool Call 會使用強制即時更新，不受 `PROGRESS_VISIBLE_AFTER_MS` 限制。

## 六、主要測試

```text
外部 WebFetch 內容含 function_results
→ Tool Result 中無活動標記

Base 最終答案留在 Thinking 且含 function_results
→ 執行一次 Repair
→ Claude Code 只收到修正後 Text

Repair 後仍含控制標記
→ 回 final_response_protocol_mismatch

Web-only 快速直接回答
→ 不建立 Progress Block

第一輪快速產生 WebSearch
→ 即使顯示門檻為 60 秒，也立即顯示正在搜尋

診斷 Log
→ 不含 System Prompt、網頁內容或污染答案原文
```

## 七、相容性

不新增 ENV，也不改變既有：

```text
VLLM_BASE_* Timeout
WEB_FETCH_URL／WEB_FETCH_API_KEY
PROGRESS_HEARTBEAT_MS
PROGRESS_VISIBLE_AFTER_MS
SSE_DRAIN_TIMEOUT_MS
```

Plain Anthropic Request 仍走 byte-transparent bypass。PDF／圖片與 Managed Web Tool 才進入轉換流程。
