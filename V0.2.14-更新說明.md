# VLLM-CC-TOOLS-PROXY V0.2.14 更新說明

## 目的

V0.2.14 修正 Managed Final Repair 可能過早結束 Claude Code 連續任務的風險。

舊版只要主模型沒有輸出 `tool_use`、而且可見文字為空，就可能把 thinking-only 回應視為最終答案異常，接著移除全部 Tools 並強制回答。若 Thinking 其實是尚未完成的下一步規劃，這會中斷原本的 Agent Workflow。

## Recovery Routing

### Final-channel recovery

只有符合以下保守條件時使用：

- Thinking 中已有足量內容；
- 具備標題、條列或多段完整答案結構；
- 沒有搜尋、讀取、驗證、比較、下一步或證據不足等繼續行動意圖。

此路徑會：

- 使用短 Context；
- 移除 Tools 與 Tool Choice；
- 傳入 `chat_template_kwargs.enable_thinking=false`；
- 只要求把既有答案移到可見 Text Channel；
- 禁止新增事實或呼叫工具。

### Continuation recovery

以下情況一律使用 Continuation：

- 未完成思考；
- 下一步規劃；
- 結構化行動計畫；
- 缺少可見輸出；
- 無法可靠判定是否已完成；
- 協議異常但仍可能需要後續動作。

此路徑保留：

- 原始對話；
- WebSearch／WebFetch 證據；
- 完整 Tools；
- 原始 Tool Choice。

模型必須在 Recovery 中產生一個有效下一步：

- 資料不足：直接輸出 Tool Call；
- 任務完成：直接輸出 Final Text；
- 不得只停在 Thinking。

## Recovered Tool Call

Recovery 產生的 WebSearch／WebFetch 不會直接返回，而是重新進入 Proxy 的正常 Managed Loop 執行。

Recovery 產生的 Read、Bash、Edit、Task 或其他非 Managed Tool，會原樣交還 Claude Code 執行。

因此以下連續流程不會因 Recovery 被強制收尾：

```text
思考
→ WebSearch
→ 思考
→ WebFetch
→ 思考
→ Read / Bash / Edit / Task
→ 思考
→ 最終回答
```

## 有界 Recovery

每一個異常 Base Response 最多只執行一次 Recovery。Recovery 後仍沒有正常 Text 或 Tool Call 時，回傳：

```text
response_recovery_exhausted
```

不會無限續接 Thinking。

## Log

V0.2.13 的時間命名診斷檔保持不變。主 Log 新增安全欄位：

```text
recovery_route=final_channel|continuation
tools_preserved=true|false
recovery_signals=...
disposition=managed|unmanaged
```

不會把完整異常內容重新展開到主 Log。

## 設定

V0.2.14 不新增 ENV。既有 `LOG_PROTOCOL_SNIPPETS` 行為維持不變。
